{
  "version": 2,
  "builds": [
    { "src": "backend/**", "use": "@vercel/node" },
    { "src": "react-frontend/build", "use": "@vercel/static" }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "backend/api/$1" },
    { "src": "/(.*)", "dest": "react-frontend/build/$1" }
  ]
}
